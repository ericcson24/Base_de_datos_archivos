services:
  # --- Frontend (React) ---
  frontend:
    build: ./front
    networks:
      - app-network

  # --- API Gateway ---
  gateway:
    build: ./gateway
    ports:
      - "8080:80"
    depends_on:
      - frontend
      - auth-service
      - notification-service
      - outlook-service
      - file-service
      - user-service
      - admin-service
      - email-service
      - ai-service
      - rdp-service
      - roadmap-service
    networks:
      - app-network

  # --- AI Service (5009) ---
  ai-service:
    build: ./services/ai-service
    environment:
      - DB_TYPE=postgres
      - DB_HOST=postgres
      - DB_USER=${DB_USER:-admin}
      - DB_PASSWORD=${DB_PASSWORD:-admin123}
      - DB_NAME=${DB_NAME:-server_db}
      - PORT=5009
      - JWT_SECRET=${JWT_SECRET}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ../Datos:/app/uploads
      - ../Tests/test_data:/app/test_data
    depends_on:
      - postgres
    networks:
      - app-network

  # --- Roadmap Service (5010) ---
  roadmap-service:
    build: ./services/roadmap-service
    environment:
      - DB_TYPE=postgres
      - DB_HOST=postgres
      - DB_USER=${DB_USER:-admin}
      - DB_PASSWORD=${DB_PASSWORD:-admin123}
      - DB_NAME=${DB_NAME:-server_db}
      - PORT=5010
      - JWT_SECRET=${JWT_SECRET}
      - OUTLOOK_SERVICE_URL=http://outlook-service:5003
      - NOTIFICATION_SERVICE_URL=http://notification-service:5002
    depends_on:
      - postgres
    networks:
      - app-network

  # --- Postgres Database (Production) ---
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: ${DB_USER:-admin}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-admin123}
      POSTGRES_DB: ${DB_NAME:-server_db}
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./postgres-init:/docker-entrypoint-initdb.d
    networks:
      - app-network

  # --- Auth Service (5001) ---
  auth-service:
    build: ./services/auth-service
    environment:
      - DB_TYPE=postgres
      - DB_HOST=postgres
      - DB_USER=${DB_USER:-admin}
      - DB_PASSWORD=${DB_PASSWORD:-admin123}
      - DB_NAME=${DB_NAME:-server_db}
      - PORT=5001
      - JWT_SECRET=${JWT_SECRET}
      - MICROSOFT_CLIENT_ID=${MS_CLIENT_ID}
      - MICROSOFT_CLIENT_SECRET=${MICROSOFT_CLIENT_SECRET}
      - MICROSOFT_REDIRECT_URI=${MICROSOFT_REDIRECT_URI:-http://localhost/api/auth/microsoft/callback}
      - DB_PATH=/data/server.db
    volumes:
      - ../database_storage:/data
      - ../database_storage/avatars:/app/uploads
    networks:
      - app-network

  # --- Notification Service (5002) ---
  notification-service:
    build: ./services/notification-service
    environment:
      - PORT=5002
      - JWT_SECRET=${JWT_SECRET}
      - REDIS_HOST=redis
      - DB_TYPE=postgres
      - DB_HOST=postgres
      - DB_USER=${DB_USER:-admin}
      - DB_PASSWORD=${DB_PASSWORD:-admin123}
      - DB_NAME=${DB_NAME:-server_db}
    depends_on:
      - redis
      - postgres
    networks:
      - app-network

  # --- Outlook Service (5003) ---
  outlook-service:
    build: ./services/outlook-service
    environment:
      - DB_TYPE=postgres
      - DB_HOST=postgres
      - DB_USER=${DB_USER:-admin}
      - DB_PASSWORD=${DB_PASSWORD:-admin123}
      - DB_NAME=${DB_NAME:-server_db}
      - PORT=5003
      - JWT_SECRET=${JWT_SECRET}
      - MS_CLIENT_ID=${MS_CLIENT_ID}
      - MS_CLIENT_SECRET=${MICROSOFT_CLIENT_SECRET}
      - DB_PATH=/data/server.db
    volumes:
      - ../database_storage:/data
    networks:
      - app-network

  # --- File Service (5004) ---
  file-service:
    build: ./services/file-service
    environment:
      - DB_TYPE=postgres
      - DB_HOST=postgres
      - DB_USER=${DB_USER:-admin}
      - DB_PASSWORD=${DB_PASSWORD:-admin123}
      - DB_NAME=${DB_NAME:-server_db}
      - PORT=5004
      - JWT_SECRET=${JWT_SECRET}
      - DB_PATH=/data/server.db
      - UPLOAD_DIR=/app/uploads
    volumes:
      - ../database_storage:/data
      - ../Datos:/app/uploads
    networks:
      - app-network

  # --- User Service (5005) ---
  user-service:
    build: ./services/user-service
    environment:
      - DB_TYPE=postgres
      - DB_HOST=postgres
      - DB_USER=${DB_USER:-admin}
      - DB_PASSWORD=${DB_PASSWORD:-admin123}
      - DB_NAME=${DB_NAME:-server_db}
      - PORT=5005
      - JWT_SECRET=${JWT_SECRET}
      - DB_PATH=/data/server.db
    volumes:
      - ../database_storage:/data
    networks:
      - app-network

  # --- Admin Service (5006) ---
  admin-service:
    build: ./services/admin-service
    environment:
      - DB_TYPE=postgres
      - DB_HOST=postgres
      - DB_USER=${DB_USER:-admin}
      - DB_PASSWORD=${DB_PASSWORD:-admin123}
      - DB_NAME=${DB_NAME:-server_db}
      - PORT=5006
      - JWT_SECRET=${JWT_SECRET}
      - DB_PATH=/data/server.db
      - EMAIL_SERVICE_URL=http://email-service:5007
      - AUTH_SERVICE_URL=http://auth-service:5001
    volumes:
      - ../database_storage:/data
    networks:
      - app-network

  # --- Email Service (5007) ---
  email-service:
    build: ./services/email-service
    environment:
      - PORT=5007
      - SMTP_HOST=${SMTP_HOST}
      - SMTP_PORT=${SMTP_PORT}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
    networks:
      - app-network

  # --- RDP Service (5008) ---
  rdp-service:
    build: ./services/rdp-service
    restart: unless-stopped
    expose:
      - "5008"
    environment:
      - PORT=5008
      - GUACD_HOST=guacd
      - GUACD_PORT=4822
      - JWT_SECRET=${JWT_SECRET}
      - DB_HOST=postgres
      - DB_USER=${DB_USER:-admin}
      - DB_PASSWORD=${DB_PASSWORD:-admin123}
      - DB_NAME=${DB_NAME:-server_db}
      - DB_PORT=5432
      - RDP_SERVER_HOST=${RDP_SERVER_HOST:-host.docker.internal}
      - RDP_SERVER_PORT=${RDP_SERVER_PORT:-3389}
    depends_on:
      - guacd
      - postgres
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - rdp_shared:/shared
    networks:
      - app-network

  # --- Guacamole Daemon ---
  guacd:
    image: guacamole/guacd
    restart: unless-stopped
    expose:
      - "4822"
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 4822 || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    volumes:
      - rdp_shared:/shared
    networks:
      - app-network
    extra_hosts:
      - "host.docker.internal:host-gateway"

  # --- Redis (Message Broker) ---
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    networks:
      - app-network

  # --- Cloudflare Tunnel (Global Access) ---
  tunnel:
    image: cloudflare/cloudflared:latest
    command: tunnel --config /etc/cloudflared/config.yml run
    environment:
      - TUNNEL_TOKEN=${TUNNEL_TOKEN}
    volumes:
      - ./cloudflared-config.yml:/etc/cloudflared/config.yml:ro
    restart: unless-stopped
    networks:
      - app-network
    depends_on:
      - gateway

volumes:
  postgres_data:
  rdp_shared:

networks:
  app-network:
    driver: bridge
